%li{:id => "issue_#{issue.id}", :data => { :id => issue.id }}
  = issue.number
  = link_to issue.title , edit_project_issue_path(@project,issue)

  .pull-right
    - issue.labels.each do |label|
      %span.label.label-info= label.name
%li.issue{:id => "issue_#{issue.id}", :data => { :id => issue.id }}
  = image_tag issue.assignee.avatar, :size => '25x25' if issue.assignee
  = issue.number
  = link_to issue.title , issue.github_url

  .pull-right
    = render issue.labels
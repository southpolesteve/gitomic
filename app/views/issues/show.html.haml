%h3 #{@issue.number} - #{@issue.title}

%h4= @issue.title
= GitHub::Markdown.render_gfm(@issue.body).html_safe

%hr
%ul.unstyled.comments
  = render @issue.comments

= render 'comments/form', project: @project, issue: @issue, comment: @comment
%h1 #{@project.owner}/#{@project.name}

= link_to "New Issue", new_project_issue_path(@project), :class => "btn"

.row
  .span4
    %h3 Backlog
    %ul.sortable.unstyled{:id => "backlog"}
      = render @backlog
  .span4
    %h3 Icebox
    %ul.sortable.unstyled{:id => "icebox"}
      = render @icebox
    
